defaults:
    - task: cls
    - model: ssg
    - task_model: ${defaults.0.task}-${defaults.1.model}

hydra:
  run:
    dir: outputs

gpus:
    - 0

input: unkown

optimizer: ???

task_model: ???

model: ???

distrib_backend: dp
